<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE
        mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.hivebiuseractive.dao.IPUserDao">

    <select id="getData" resultType="IPUser">
        select * from bi_hour_usc where dt=#{dt}
    </select>

</mapper>